{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -n \"$(git status --porcelain)\" ]; then git add -A && files=$(git diff --cached --name-only | head -4 | tr '\\n' ' ' | sed 's/ $//') && git commit -m \"auto-save: $files\" && git push origin $(git rev-parse --abbrev-ref HEAD) && echo \"[auto-commit] pushed: $files\"; fi"
          }
        ]
      }
    ]
  }
}
